<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:name="com.example.notes.Fragments.AlbumsFragment"
    android:orientation="vertical" android:layout_width="match_parent"
    android:padding="16dp"
    android:layout_height="match_parent"
    android:id="@+id/fragment_Albums">
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerview_Albums"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <ProgressBar
        android:id="@+id/progressBar_albumsFragment"
        style="?android:attr/progressBarStyle"
        android:layout_width="match_parent"
        android:layout_gravity="center"
        android:progressDrawable="@drawable/custom_progressbar"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/textview_emptyAlbums"
        android:text="We could not find any albums."
        android:layout_width="wrap_content"
        android:visibility="gone"
        android:gravity="center"
        android:layout_height="wrap_content"/>

</LinearLayout>