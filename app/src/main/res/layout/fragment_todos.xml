<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:name="com.example.notes.Fragments.TodosFragment"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:id="@+id/fragment_Todos">
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerview_Todos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <ProgressBar
        android:id="@+id/progressBar_todosFragment"
        style="?android:attr/progressBarStyle"
        android:layout_width="match_parent"
        android:progressDrawable="@drawable/custom_progressbar"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/textview_emptyTodos"
        android:text="We could not find any todos."
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:gravity="center"
        android:layout_height="wrap_content"/>

</LinearLayout>